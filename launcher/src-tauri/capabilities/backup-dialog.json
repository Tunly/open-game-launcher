{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "backup-dialog",
  "description": "Allow the main window to pick a local backup target folder.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
