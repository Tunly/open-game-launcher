{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher-updater-windows",
  "description": "Allow only the main Windows launcher window to check, install, and restart after signed OG Launcher updates.",
  "platforms": ["windows"],
  "windows": ["main"],
  "permissions": [
    "updater:allow-check",
    "updater:allow-download-and-install",
    "process:allow-restart"
  ]
}
