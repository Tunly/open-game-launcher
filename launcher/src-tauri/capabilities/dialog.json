{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Allow the main window to open native file and folder pickers.",
  "windows": ["main"],
  "permissions": ["dialog:allow-open"]
}
