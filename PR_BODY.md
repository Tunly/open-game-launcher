## Phase 4 — Test Coverage

Adds 84 new tests (89 → 173) across components, stores, hooks, and Supabase helpers. All checks green: `typecheck`, `lint`, `test`.

### Summary

| Area | Before | After |
|------|--------|-------|
| Tests | 89 | **173** |
| Test files | 5 | **13** |
| Coverage in-scope | n/a | **92–100%** per file |

### Test files added

- `components/ui/Button.test.tsx` (5)
- `components/ui/ConfirmDialog.test.tsx` (4)
- `components/library/PlatformIcons.test.tsx` (6)
- `components/library/LibrarySidebar.test.tsx` (8)
- `stores/downloadStore.test.ts` (15)
- `stores/modInstallStore.test.ts` (14)
- `hooks/useDebouncedValue.test.ts` (4) + new hook
- `hooks/__tests__/useLocalStorageState.test.tsx` (extended: 6 → 9)
- `lib/supabase/__tests__/helpers.test.ts` (25)

### Infra

- Vitest: coverage scope extended to UI / stores / hooks; `lcov` reporter added
- `test/setup.ts`: global `@tauri-apps/api/core` mock, `matchMedia`/`ResizeObserver`/`IntersectionObserver` stubs, `@testing-library/react` `cleanup` after each test
- `test/mocks/supabase.ts`: reusable Supabase fake client (for future converter/integration tests)
- `ROADMAP.md`: new top-level plan tracking all 6 improvement phases

### Lint / type fixes (blockers for green CI)

- Removed unused imports in `lib/supabase/profile/_shared.ts`
- Removed unused imports in `lib/supabase/profile/schemas.ts` (kept the `Friendship` import that is actually used)
- Cleaned unused `it.each` params in `PlatformIcons.test.tsx`
- Adjusted `LibraryAdvancedFilters` shape in test fixture to match current interface

### Hygiene

- `launcher/.gitignore`: now excludes `coverage/` reports (regenerated by `pnpm test:cov`)
- Untracked generated `launcher/coverage/` HTML report

### Out of scope (for later phases)

- Phase 1 (Security): Stripe auth bug, RLS gaps, shell/path-injection — tracked in `ROADMAP.md`
- Phase 3 (LibraryPage refactor): 2749-LOC god-component — separate branch `feature/phase-3-librarypage-refactor`
- Phase 2 (Tooling): Prettier, Husky, tsconfig strictness — separate scope

### Verification

```bash
cd launcher
pnpm typecheck   # ✅
pnpm lint        # ✅
pnpm test        # ✅ 173 passed
pnpm test:cov    # in-scope: 92–100% lines
```
